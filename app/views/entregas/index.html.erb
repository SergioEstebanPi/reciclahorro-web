<p id="notice"><%= notice %></p>

<h1>Entregas</h1>

<table class="table table-bordered table-hover">
  <thead class="thead-dark">
    <tr>
      <th>Vecino</th>
      <th>Estado</th>
      <th>Peso</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @entregas.each do |entrega| %>
      <tr>
        <td><%= entrega.vecino.doc_identidad %></td>
        <td><%= entrega.estado %></td>
        <td><%= entrega.peso %></td>
        <td><%= link_to 'Ver', entrega %></td>
        <td><%= link_to 'Editar', edit_entrega_path(entrega) %></td>
        <td><%= link_to 'Eliminar', entrega, method: :delete, data: { confirm: 'Estás seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Crear Entrega', new_entrega_path %>
