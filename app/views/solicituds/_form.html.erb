<%= form_with(model: solicitud, local: true) do |form| %>
  <% if solicitud.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(solicitud.errors.count, "error") %> prohibited this solicitud from being saved:</h2>

      <ul>
      <% solicitud.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :titulo %>
    <%= form.text_field :titulo, id: :solicitud_titulo %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_area :descripcion, id: :solicitud_descripcion %>
  </div>

  <div class="field">
    <%= form.label :fecha_recoleccion %>
    <%= form.date_select :fecha_recoleccion, id: :solicitud_fecha_recoleccion %>
  </div>

  <div class="field">
    <%= form.label :usuario_id %>
    <%= form.text_field :usuario_id, id: :solicitud_usuario_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
