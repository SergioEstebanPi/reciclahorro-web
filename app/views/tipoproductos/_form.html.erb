<%= form_with(model: tipoproducto, local: true) do |form| %>
  <% if tipoproducto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipoproducto.errors.count, "error") %> prohibited this tipoproducto from being saved:</h2>

      <ul>
      <% tipoproducto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :tipoproducto_nombre %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_area :descripcion, id: :tipoproducto_descripcion %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
